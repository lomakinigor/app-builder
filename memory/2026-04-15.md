# 2026-04-15

## Сессия 1 (08:35)
Тема: T-011 — IdeaPage + ResearchPage acceptance tests

### Сделано
- Написаны `src/pages/idea/IdeaPage.acceptance.test.tsx` (24 теста)
- Написаны `src/pages/research/ResearchPage.acceptance.test.tsx` (29 тестов)
- Всего +53 acceptance теста; весь suite 829/829 ✓

### Решения
- Textarea в IdeaPage не привязана к label через `htmlFor`/`id` — нашли по `getByPlaceholderText(/A project management tool/)`
- Текст gate-баннера на ResearchPage = реальная строка из `canAdvanceFromIdea`, не fallback — использовали `/Идея ещё не сохранена/`
- Кнопки вкладок содержат emoji ("🔬 Новое исследование") — использовали regex `/Новое исследование/`
- Паттерн мока стора: `mockUseProjectStore.mockImplementation((selector?) => { const state = makeStore(...); return selector ? selector(state) : state })`

### Покрытые сценарии
- A. No project guard → EmptyState, CTA, navigate /project/new
- B. Idea form empty state → textarea visible, save-draft disabled
- C. Idea blocking → validation errors after submit, no navigate, no setIdeaDraft
- D. Idea happy path → pre-populated from store, continue → setIdeaDraft + navigate('/research')
- E. Persistence contract → stored idea text reflected in form
- ResearchPage A–F: entry guard, idea gate banner, research empty state, happy path с brief, blocking с пустым problemSummary, cross-stage acceptance

### TODO на потом
- T-302 — BlogPage UI tests (T-301 paired test)
- T-011 статус now: done
- T-012 — Import normalizer edge cases
