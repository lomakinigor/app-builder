# 2026-04-17

## Сессия 1 (начало сессии)
Тема: T-018 визуальные регрессии HistoryPage (продолжение из предыдущего контекста) — завершена в прошлой сессии, коммит 16c16b5

## Сессия 2
Тема: T-019 — Проверка sound notification playback path

### Сделано

- Создан `tests/e2e/helpers/audioMonitor.ts`:
  - `injectAudioMonitor(page)` — патчит `AudioContext.prototype.createOscillator` через `addInitScript`, каждый `OscillatorNode.start()` пишет в `window.__audioPlayLog`
  - `getPlayedSounds(page)`, `clearPlayedSounds(page)`, `waitForSoundAttempt(page, ms)`
  - Пишет `{ timestamp, type: 'oscillator-start', contextState }` — contextState позволяет диагностировать suspended/running

- Создан `tests/e2e/sound-notifications.spec.ts`:
  - SOUND-001: sound ON → click "Проверить звук" → oscillator-start fired ✅
  - SOUND-002: sound OFF → preview button скрыт + log пустой ✅
  - SOUND-003: toggle OFF→ON round-trip → preview fires ✅

- Результаты: 3/3 на 4 прогонах, стабильно

### Решения / Root cause

**Нет production bug.** Код корректен.

Диагноз:
- В Playwright Chromium: `contextState = 'running'` — AudioContext стартует в running state потому что Playwright не имеет autoplay ограничений
- В реальном браузере: AudioContext создаётся ПОСЛЕ user gesture (пользователь кликнул "Generate" / "Parse"), поэтому Chrome создаёт его в running state
- Код в `playBeep()` правильно обрабатывает suspended state через `resume().then(doBeep)`
- `settingsStore.onRehydrateStorage` корректно синкает `soundEnabled` при hydration

**"Звука нет"** = observation в тестовой среде (нет реальных speakers/headphones), не production bug.

### TODO на потом

- RTL-level coverage для `playTestBeep` + `setSoundNotificationsEnabled` в SettingsPage (smoke)
- UI indicator "sound blocked by browser" если `ctx.state` остаётся `suspended` после `resume()`
- Добавить SOUND-004: PromptLoop trigger path (awaiting_confirmation через Generate First Prompt)

## Сессия 3
Тема: T-020 — Promote E2E-001 to required CI gate

### Сделано

- Добавлен npm script `test:e2e:critical` в package.json — запускает только `tests/e2e/happy-path.spec.ts`
- Создан `.github/workflows/e2e-critical.yml`:
  - job name `e2e-critical` (стабильный, подходит для branch protection)
  - запускается независимо от deploy.yml и Netlify
  - на failure загружает trace + screenshots в artifact `e2e-critical-report` (14 дней)
  - один retry в CI (как в playwright.config.ts)
- Обновлён `docs/tasks.md` — добавлен T-020 (ops, done)
- Обновлён `docs/plan.md` — добавлена строка T-020 в таблицу
- Обновлён `docs/testing-strategy.md` — новый раздел "Critical E2E gate (T-020)" с таблицей scope и инструкцией по branch protection

### Валидация

`npm run test:e2e:critical` — 2 запуска, оба прошли:
- Run 1: 1 passed (44.6s)
- Run 2: 1 passed (19.4s)

### Решения

- Таргетинг по явному пути файла (`tests/e2e/happy-path.spec.ts`) — самый простой и читаемый вариант, без grep/тегов
- Отдельный workflow (`e2e-critical.yml`) вместо job в существующем `e2e.yml` — чтобы имя статус-чека было стабильным (`e2e-critical`) и не смешивалось с full suite
- Full suite (`e2e.yml`) остаётся informational (не блокирует мержи)
- Netlify/deploy — не трогали, out of scope

### Manual step (требует repo admin)

GitHub → Settings → Branches → main → Require status checks → добавить **`e2e-critical`**

### TODO на потом

- Настроить branch protection (manual repo admin step)
- Netlify токен (NETLIFY_AUTH_TOKEN) — обновить в GitHub Secrets

## Сессия 4
Тема: T-021 — Desktop visual baseline для PromptLoopPage summary

### Сделано

- Добавлен `makePromptLoopIteration()` в seedState.ts — итерация со статусом 'parsed', inferredNextPhase='review', hasTests=true, targetTaskId='T-001' → рендерит полный summary view
- Добавлен `seedPromptLoopPage(page, 'application')` в seedState.ts — полный pipeline (project, spec, arch, 1 parsed iteration) + navigate /prompt-loop
- Создан `tests/visual/prompt-loop-summary.visual.spec.ts`:
  - VIS-003: application project, parsed iteration, fullPage screenshot
  - 2 sanity assertions: heading 'Цикл промптов' + badge 'Распарсено'
- Baseline PNG создан: `promptloop-summary-desktop-visual-chromium-linux.png`
- Обновлён `docs/testing-strategy.md` — таблица baseline файлов, npm run test:visual:update инструкция
- Обновлён `docs/tasks.md` — T-021 (ops, done)
- Обновлён `docs/plan.md` — T-021 строка

### Валидация

npm run test:visual (все 3 визуальных теста):
- VIS-001 ✓ (HistoryPage app)
- VIS-002 ✓ (HistoryPage web)
- VIS-003 ✓ 3/3 запуска, нулевые диффы

### Решения

- Используем `seedPromptLoopPage('application')` — application тип показывает больше (SPA-специфичный промпт, CycleContextBar полностью заполнен)
- `status: 'parsed'` + `inferredNextPhase: 'review'` → оба кнопки "Код+Тесты" и "Ревью: T-001" видны
- fullPage: true — нет необходимости обрезать; sidebar + content composition визуально важны

### CI stance

Визуальные тесты non-blocking (не добавлены в branch protection), запускаются через e2e.yml.

### TODO на потом

- T-022 (следующая задача по roadmap)

## Сессия 5
Тема: T-022 — SpecPage structural RTL quality slice

### Сделано

- Создан `src/pages/spec/SpecPage.structural.test.tsx` — 26 тестов в 5 группах:
  - A. Structural headings — все 5 заголовков секций (Резюме продукта, Объём MVP, Список фич, Допущения, Ограничения) + текстовые значения
  - B. MoSCoW priority labels — все 4 значения (Обязательно/Желательно/По возможности/Не сейчас), имена фич, негативный сценарий
  - C. Assumptions and constraints — оба пункта каждого списка, пустой массив
  - D. acceptanceNotes — показан при непустом значении, скрыт при пустой строке
  - E. Edit mode — вход по "Редактировать спек", наличие Save/Cancel, Cancel возвращает в view mode

- Обновлён `docs/tasks.md` — добавлен T-022 (test, done)
- Обновлён `docs/plan.md` — добавлена строка T-022

### Валидация

- 26/26 новых тестов pass
- Все 79 тестов src/pages/spec/ pass (нет регрессий в T-013, T-106)
- Full suite: 1292/1292 pass, 34 файла

### Решения

- Gap: SpecPage.test.tsx (T-013) и SpecPage.type-aware.test.tsx (T-106) покрывали guards/gates и type-specific content, но не структурные секции EditableSpecPack view mode и edit/cancel flow
- `makeRichSpec()` — локальный фикстур с 4 фичами (все 4 MoSCoW-уровня) + все optional поля заполнены
- Использованы только семантические RTL-запросы (getByText, getByRole, getAllByRole) — без test-id
- EditableSpecPack в edit-mode рендерит 2 кнопки "Сохранить" (верхний баннер + нижний ряд) → использован getAllByRole().length

### TODO на потом

- T-023 (следующая задача)

## Сессия 7
Тема: T-024 — Surface blocked sound state in SettingsPage

### Сделано

- Обновлён `src/shared/lib/attentionSignal.ts`:
  - Экспортирован тип `PlayBeepResult = 'played' | 'blocked' | 'unavailable'`
  - `playTestBeep()` стал `async function playTestBeep(): Promise<PlayBeepResult>`
  - Логика: `!Ctor` → 'unavailable'; suspended + resume() throws → 'blocked'; suspended + state не стал 'running' после resume() → 'blocked'; иначе → doBeep() + 'played'
  - `playBeep()` (используется внутри сигнального цикла) не изменён

- Обновлён `src/pages/settings/SettingsPage.tsx`:
  - Добавлен `beepBlocked` state
  - `handlePreviewBeep()` — async handler: сбрасывает blocked, ожидает результат, ставит blocked если 'blocked'
  - Добавлен `<p role="status">` с текстом "Браузер заблокировал воспроизведение..." при `beepBlocked=true`

- Обновлён `src/shared/lib/attentionSignal.test.ts` — добавлена группа G (6 тестов):
  - 16: running → 'played'
  - 17: нет AudioContext → 'unavailable'
  - 18: sound disabled → 'unavailable'
  - 19: suspended + resume() успешно → 'played'
  - 20: suspended + resume() throws → 'blocked'
  - 21: suspended + resume() resolves но state остался suspended → 'blocked'

- Обновлён `src/pages/settings/SettingsPage.test.tsx` — добавлена группа F (5 тестов):
  - 14: blocked result → role="status" появляется (waitFor)
  - 15: blocked result → текст содержит /заблокировал/i
  - 16: played result → role="status" отсутствует (act)
  - 17: unavailable result → role="status" отсутствует (act)
  - 18: blocked → played retry → message очищается

- Обновлён `docs/tasks.md` — T-024 (impl+test, done)
- Обновлён `docs/plan.md` — строка T-024

### Валидация

- 39/39 в двух файлах (attentionSignal + SettingsPage)
- Full suite: 1305/1305 (было 1294, +11 новых тестов)

### Решения

- Оставил `playBeep(): void` нетронутым — используется в сигнальном цикле, не нужен результат
- `playTestBeep()` — отдельная реализация (не делегирует в playBeep), чтобы иметь полный контроль над async flow и возвратом результата
- `role="status"` — live region ARIA (polite), правильный семантический выбор для non-urgent статусных сообщений
- Для new async tests: `waitFor` для blocked (ждём появления), `act(async)` для played/unavailable (ждём завершения без появления элемента)

### TODO на потом

- SOUND-004: E2E тест для trigger path через PromptLoop (awaiting_confirmation)
- T-025 (следующая задача по roadmap)

## Сессия 8
Тема: SOUND-004 — E2E для awaiting_confirmation attentionSignal path в PromptLoop

### Сделано

- Создан `tests/e2e/sound-awaiting-confirmation.spec.ts`:
  - SOUND-004: seed project с spec+arch (без iterations) + soundEnabled=true → navigate /prompt-loop → click "Сгенерировать первый промпт" → wait для "Итерация 1" heading → waitForSoundAttempt → assert log.length > 0 → fill textarea → wait 300ms → assert count не вырос
  - Reuse: `injectAudioMonitor`, `getPlayedSounds`, `waitForSoundAttempt` из audioMonitor.ts
  - Inline seed (без iterations) — отличие от seedPromptLoopPage который seed'ит parsed iteration

- Обновлён `docs/tasks.md` — добавлен SOUND-004 (test, done)
- Обновлён `docs/plan.md` — добавлена строка SOUND-004
- Обновлён `docs/testing-strategy.md` — добавлен раздел "Sound notification test coverage" с таблицей всех sound тестов (SOUND-001..004 + attentionSignal unit + SettingsPage RTL)

### Решения

- Stop condition: fill textarea (onChange → stopAttentionSignal('awaiting_confirmation') в PromptLoopPage.tsx)
- Post-stop assertion window: 300ms (безопасно т.к. SIGNAL_INTERVAL_MS=15000ms)
- Inline seed вместо reuse seedPromptLoopPage — нужен стейт БЕЗ iterations (кнопка generate активна)
- Тест не запускался локально (missing libatk-1.0.so.0 на этой машине) — то же ограничение что и для E2E-001 и T-019 тестов; логика валидирована структурно по аналогии с passing T-019 тестами

### Vitest suite

- 1303/1305 pass (было 1305/1305) — 2 pre-existing timeout flakes (BlogPage.acceptance + SpecPage.test), не связаны с SOUND-004
- SOUND-004 файл — Playwright E2E, не в Vitest count

### TODO на потом

- T-025 (следующая задача по roadmap)

## Сессия 6
Тема: T-023 — SettingsPage sound toggle and preview RTL smoke

### Сделано

- Обнаружено: `SettingsPage.test.tsx` (PLAT-ALERT-002) уже покрывал:
  - A. Toggle wiring: setSoundNotificationsEnabled(false/true) — тесты 5, 6, 7
  - B. Preview beep: playTestBeep вызван 1 раз — тест 11
  - Единственный gap: сценарий C (guard — кнопка не отображается при sound=false)

- Добавлены 2 теста в `src/pages/settings/SettingsPage.test.tsx`:
  - Тест 12: preview button NOT in DOM when soundNotificationsEnabled=false
  - Тест 13: playTestBeep spy чист (не вызван) когда sound disabled

- Обновлён `docs/tasks.md` — добавлен T-023 (test, done)
- Обновлён `docs/plan.md` — добавлена строка T-023

### Валидация

- 13/13 в SettingsPage.test.tsx
- Full suite: 1294/1294 (было 1292 до T-023, +2 новых теста)

## Сессия 9
Тема: T-102/T-106 — Type-aware spec generation tests (аудит + закрытие)

### Сделано

- Установлено, что `src/mocks/services/specService.type-aware.test.ts` уже существует (90 тестов, создан в более ранней сессии без фиксации в docs)
- 90/90 тестов проходят: `npm test -- src/mocks/services/specService.type-aware.test.ts`
- Закрыт T-106 в `docs/tasks.md` — добавлен DoD с описанием 6 групп (A–F), статус → done
- Обновлён `docs/plan.md` — T-106 row → done
- Добавлен раздел "Type-aware spec and architecture tests (T-106)" в `docs/testing-strategy.md`

### Уточнение по задаче

- Пользователь назвал задачу "T-102", но по docs T-102 = store behavior (setProjectType), а T-106 = type-aware spec/arch generation
- `specService.type-aware.test.ts` файл покрывает именно T-106 acceptance criteria → закрыт T-106
- T-102 (store behavior tests) — отдельная, ещё не реализованная задача

### Покрытие в specService.type-aware.test.ts

- A: spec differentiation (featureList, constraints, assumptions, summary, MVPScope, acceptanceNotes) — 10 тестов
- B: arch differentiation (stack, moduleArchitecture, dataFlow, roadmap, risks) — 11 тестов
- C: minimal contract per type (все поля, gate passes) via it.each — 18 тестов
- D: brief integration (valueHypothesis, recommendedMVP, targetUsers) via it.each — 12 тестов
- E: fallback unknown type (no throw, shape pin, gate) — 6 тестов
- F: determinism (identical inputs → identical outputs) — 10 тестов

### TODO на потом

- T-102 (store behavior): setProjectType unit tests, persist round-trip, null safety
