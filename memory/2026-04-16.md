# 2026-04-16

## Сессия 7 (11:00)
Тема: PLAT-ALERT-001 — Platform attention signal helper

### Сделано
- Создан `src/shared/lib/attentionSignal.ts` — singleton helper с Web Audio API
- Создан `src/shared/lib/attentionSignal.test.ts` — 15 тестов (A-F), 15/15 ✓
- Создан `src/shared/lib/attentionSignalVSCodeAdapter.ts` — VS Code webview адаптер
- Создан `docs/platform-attention-signals.md` — developer contract
- Интегрирован в ResearchPage, SpecPage, ArchitecturePage, PromptLoopPage
- Итог: 1240 → 1255 тестов; 0 регрессий

### Решения
- `awaiting_confirmation` (приоритет 2) > `task_completed` (приоритет 1): более низкий приоритет не прерывает активный высокий
- Web Audio: AudioContext — singleton в модуле; при `state === 'suspended'` → `resume()` + defer; при отсутствии API — silent fallback
- Тест mock-ловушка: `vi.fn(() => ctx)` нельзя как конструктор (arrow function); исправлено на `vi.fn(function() { return ctx })`
- VS Code adapter: `acquireVsCodeApi` → `postMessage` к host для status bar; `message` listeners для `attention/confirmed` от host
- PromptLoop: после генерации промпта → `awaiting_confirmation`; onChange/onFocus textarea → `stopAttentionSignal('awaiting_confirmation')`; после parse → `task_completed`

### TODO на потом
- User settings UI для включения/выключения звука (setSoundNotificationsEnabled)
- Разные тоны для confirmation vs completion
- Протестировать вручную в браузере (autoplay unlock flow)

## Сессия 6 (09:50)
Тема: E2E-001-STAB — Стабилизация Playwright happy path

### Сделано
- Установлены GTK/ATK библиотеки вручную (без sudo) в `~/.local/pw-deps/` через `apt-get download + dpkg-deb -x`
- Исправлен `tests/e2e/happy-path.spec.ts` — 6 категорий проблем устранены
- Обновлён `playwright.config.ts`
- Добавлен `test:e2e:local` в `package.json`
- Результат: 10/10 `--repeat-each=10 --retries=0` ✓
- Закоммичено `56d2cf8`, запушено в main

### Найденные проблемы и исправления
1. **strict mode violations на heading** — каждая страница имеет h1 (PageHeader) и h3 (CardHeader) с одинаковым текстом. Исправление: `{ level: 1 }` на всех `getByRole('heading')`
2. **strict mode на project name** — `getByText('E2E Test Project')` → 2 элемента (sidebar + TopBar button). Исправление: `getByRole('button', { name: /E2E Test Project/ })`
3. **Дублированные CTA-кнопки** — каждая навигационная кнопка в PageHeader action slot И в основном контенте. Исправление: `.first()` на всех кнопках переходов
4. **targetTaskId = null** — `canAdvanceToReview` требует ненулевой taskId. Исправление: заполнить поле `T-001` перед генерацией первого промпта
5. **React render race** — после async state update кнопка видна но mid-render. Исправление: `await expect(btn).toBeEnabled()` перед `.click()` на всех post-generation CTAs
6. **Zustand persist hydration timing** — после `page.reload()` в шаге 6 PromptLoopPage может монтироваться до завершения hydration (architectureDraft null). Исправление: `await expect(genFirstBtn).toBeEnabled({ timeout: 15_000 })`

### Изменения config
- `timeout`: 40s → 60s
- `expect.timeout`: 10s → 15s
- Добавлен `screenshot: 'only-on-failure'`
- Добавлен `test.step()` для всех 8 стадий

### Среда
- Локально GTK/ATK libs отсутствуют (нет sudo). Решение: `apt-get download` + `dpkg-deb -x` → `~/.local/pw-deps/`
- Для запуска: `npm run test:e2e:local`
- В CI: `npx playwright install chromium --with-deps` — работает без изменений (`.github/workflows/e2e.yml`)

### TODO
- E2E-001: статус DONE (стабилен 10/10)
- Следующий шаг: E2E-001 готов стать non-blocking CI signal → через несколько успешных CI runs можно добавить в branch protection как blocking gate

## Сессия 5 (09:08)
Тема: SMOKE-RTL-001 — Cross-stage RTL smoke для HistoryPage

### Сделано
- Создан `src/pages/history/HistoryPage.cross-stage-smoke.test.tsx` (2 smoke теста)
- Всего 1238 → 1240 тестов; 2/2 ✓; 0 регрессий

### Решения
- Smoke тест = "широкий но неглубокий": один тест на тип (application + website), каждый содержит полный store snapshot (ideaDraft + specPack + architectureDraft + 2 promptIterations)
- Тип badge (`'📱 Приложение'`/`'🌐 Сайт'`) появляется в 3 местах (project card, arch card, spec card) → `getAllByText(...).length >= 1`, не `getByText`
- Никаких integration gaps не обнаружено — HistoryPage уже корректно подключён к всем slice store
- Smoke не дублирует T-110: T-110 тестирует группы по отдельности, smoke проверяет co-occurrence всех маркеров в одном рендере

### Покрытые маркеры (оба теста)
- Idea: `'Идея зафиксирована'` из CycleTimeline
- Type: badge из activeProject.projectType
- Spec: feature name + `'Спек-пакет сгенерирован'`
- Arch stack: primary (React/Next.js) + TypeScript
- Arch roadmap: `'Core flow'` / `'Core pages'`
- Prompt #1 snippet: `'## Stack'`
- Prompt #2 snippet: `'continuing the implementation'`
- Anti-contamination: 2 negative assertions на тип

### TODO на потом
- E2E-001: запустить `npx playwright install chromium` + `npm run test:e2e` 5-10 раз локально
- SMOKE-RTL-001 статус: done

## Сессия 1 (03:24)
Тема: T-106 — Type-aware SpecPage + ArchitecturePage UI assertions

### Сделано
- Создан `src/pages/spec/SpecPage.type-aware.test.tsx` (21 тест)
- Создан `src/pages/architecture/ArchitecturePage.type-aware.test.tsx` (35 тестов)
- Всего 1080 → 1136 тестов; новые 56/56 ✓; 0 регрессий
- Закоммичено `cb7c902`, запушено в main → auto-deploy на Netlify

### Решения
- T-013 покрывал guards/gates, но нулевых type-aware UI assertions — T-106 восполняет это
- SpecPage type-aware: badge `'🌐 Сайт'` / `'📱 Приложение'` (из `activeProject.projectType`), description sentence (из `specPack.projectType`), feature names (из `specPack.featureList`)
- ArchitecturePage type-aware: `StackViewRow` рендерит `item.name` напрямую → "React" / "Next.js" assertable через `screen.getByText`
- Roadmap phase titles рендерятся как `{phase.title}` → "Core flow" / "Dashboard and navigation" vs "Core pages" / "Blog"
- Для website "React" проверяется через font-semibold span filter (не просто queryByText) — так как "React Router" могло бы содержать слово "React" в rationale; но fixture чистый, проверяется через stack item names

### Покрытые группы (SpecPage — 21 тест)
- A. Type badge: application/website badge правильный, кросс-загрязнения нет
- B. Type description: видна только при наличии specPack, верна по типу
- C. Feature list: "User onboarding" / "Homepage", кросс-блид нет
- D. productSummary: рендерится, отличается по типу
- E. Gate alignment: тип не маскирует gate failure; diagnostics появляются

### Покрытые группы (ArchitecturePage — 35 тестов)
- A. Type badge
- B. Type description text
- C. Stack primary: "React" / "Next.js"
- D. TypeScript в обоих типах
- E. Кросс-стек контаминация: application NOT Next.js, website NOT React; Tailwind cross-cutting
- F. Roadmap vocabulary application: "Core flow", "Dashboard and navigation", "Onboarding screen"
- G. Roadmap vocabulary website: "Core pages", "Blog", "Article list page", "SEO and contact"
- H. Vocabulary differentiation: кросс-bleed нет
- I. Gate alignment: complete arch → CTA enabled; пустой стек/роадмап → CTA blocked + diagnostics

### TODO на потом
- T-106 статус: done
- T-108 статус: done (63 тестов, 1136→1199)
- T-110 статус: done (39 тестов, 1199→1238)
- FIX-001 статус: done (см. Сессия 4)
- E2E-001: запустить локально `npx playwright install chromium` + `npm run test:e2e` 5-10 раз

## Сессия 3 (03:50)
Тема: T-110 — Review history view (type + stack + roadmap + prompt history)

### Сделано
- Создан `src/pages/history/HistoryPage.history-view.test.tsx` (39 тестов)
- Исправлен HistoryPage.tsx: 3 wiring gaps устранены
- Всего 1199 → 1238 тестов; 39/39 ✓; 0 регрессий
- 1 тест фиксил в процессе: "Промпт-итераций пока нет" матчило 2 элемента → переключён на `getAllByText(...).length >= 1`

### Изменения в HistoryPage.tsx
1. **Arch card (новый)**: блок `{architectureDraft && ...}` показывает:
   - Stack badges с `item.name` → "React"/"Next.js"/"TypeScript"/etc.
   - Roadmap phase список с `phase.title` → "Core flow"/"Core pages"/"Blog"/etc.
   - Badge типа проекта ("📱 Приложение"/"🌐 Сайт")
2. **Feature names fix**: spec summary теперь рендерит `f.name` (не `f.id`) → "User onboarding"/"Homepage"/etc.
3. **Prompt text snippet**: `IterationReviewCard` добавляет `data-testid="prompt-text-snippet"` с первыми 500 символами `iter.promptText`

### Wiring gaps (до T-110)
- `architectureDraft` читался из стора, но рендерился только как boolean `hasArch` → стек и фазы не отображались
- `featureList` показывал только IDs (`f-001`, `f-002`), не имена (`User onboarding`, `Homepage`)
- `iter.promptText` существовал в типе, но ни разу не рендерился → cross-contamination нельзя было проверить

### Покрытые группы (39 тестов)
- A. Type & stack summary (8): project badge, "React"/"Next.js"/"TypeScript" в arch card, arch card badge
- B. Roadmap & stage history (8): "Core flow"/"Dashboard and navigation" (app), "Core pages"/"Blog"/"SEO and contact" (web), CycleTimeline stages
- C. Prompt history integration (9): "## Stack"/"## MVP scope" в первом промпте, "continuing the implementation" в следующем, no cross-contamination (app→no Next.js/SEO, web→no React Router/Zustand)
- D. Cross-type comparison (8): app has React/not Next.js, web has Next.js/not React, roadmap не перекрывается, stack items не перекрываются, feature names не перекрываются
- E. Edge states (6): no project, no arch draft, no iterations, no specPack, multiple snippets, empty promptText

### TODO на потом
- E2E-001: запустить локально `npx playwright install chromium` + `npm run test:e2e` 5-10 раз
- T-110 статус: done
- Следующий возможный шаг: вынести APP_ARCH / WEB_ARCH fixtures в shared module (используется T-104/T-108/T-110)

## Сессия 2 (03:38)
Тема: T-108 — Cycle-aware Prompt Loop tests

### Сделано
- Создан `src/mocks/services/promptService.cycle-aware.test.ts` (63 теста)
- Всего 1136 → 1199 тестов; 63/63 ✓; 0 регрессий
- Закоммичено `1eadd2b`, запушено в main → auto-deploy на Netlify

### Решения
- T-014 (engine.test.ts) покрывал только `application` type — website тип никогда не тестировался на уровне prompt text
- `generateFirstPrompt` embeds stack verbatim как `"Name — Role"` lines → assertable точно
- `generateNextPrompt` НЕ re-embeds arch stack; type context проходит только через `typeAwareGuidance(projectType)`
- arch.roadmapPhases[0] — первая фаза — это всегда то, что попадает в prompt
- Для теста разных phase titles создавались отдельные arch fixtures: APP_ARCH_CORE_FLOW, WEB_ARCH_CORE_PAGES, WEB_ARCH_BLOG
- 1 провалившийся тест: "review prompt does NOT contain TDD rule" — оказалось, review task checklist ССЫЛАЕТСЯ на "TDD rule" by name. Fixed: проверяем отсутствие `"## TDD rule (mandatory)"` block

### Покрытые группы (63 теста)
- A. generateFirstPrompt website tech context: Next.js, TypeScript, MDX, Vercel, SSG/SSR, SEO, type label
- B. Stack format "Name — Role" verbatim; no cross-contamination (Next.js absent from app, React Router absent from web)
- C. Roadmap vocabulary: Foundation, Core flow, Core pages, Blog phase titles + goals; vocabulary differs between types
- D. generateNextPrompt type-aware guidance: SPA/Zustand/React Router for app; SSG/SSR/SEO for web; no leakage
- E. Cycle-aware: Stage 5/6 labels, TDD rule presence/absence, missing-tests injection, review task section
- F. First vs Next structural differences: ## Stack section, ## MVP scope, intro sentence
- G. Combined: all 3 dimensions (type + stack + phase) together in one prompt; no bleed between types

## Сессия 4 (FIX-001)
Тема: Shared canonical fixtures for Spec/Arch/Prompt/History tests

### Сделано
- Создан `src/mocks/fixtures/archFixtures.ts` (экспорт: createAppArch, createWebArch, createAppArchCoreFlow, createWebArchCorePages, createWebArchBlog)
- Создан `src/mocks/fixtures/specFixtures.ts` (экспорт: createAppSpec, createWebSpec)
- Рефакторинг T-108 (promptService.cycle-aware.test.ts): убраны inline APP_ARCH/WEB_ARCH/APP_SPEC/WEB_SPEC/3 single-phase variants → импорт из fixtures
- Рефакторинг T-110 (HistoryPage.history-view.test.tsx): убраны inline APP_ARCH/WEB_ARCH + makeApplicationSpec/makeWebsiteSpec → импорт из fixtures
- Рефакторинг T-106 (SpecPage.type-aware.test.tsx): убраны makeApplicationSpec/makeWebsiteSpec → импорт из fixtures
- Закоммичено `fe05b0f`, 1238/1238 ✓, 0 регрессий

### Решения
- 3 файла имели копии APP_ARCH/WEB_ARCH; T-104 не имел (генерирует через сервис)
- Расхождения: T-108 had 3-phase arch; T-110 had 5-phase → канонические = 5 фаз (T-110)
- moduleArchitecture/dataFlow/technicalRisks тоже различались → взяты из T-110 (более описательные)
- Ни один тест не ассертит эти поля напрямую → zero-risk merge
- Фабрики (не константы) — каждый тест получает fresh object, мутация невозможна
- T-108 single-phase variants (CoreFlow/CorePages/Blog) теперь используют override roadmapPhases через базовую фабрику

### TODO на потом
- E2E-001: запустить локально `npx playwright install chromium` + `npm run test:e2e` 5-10 раз
- Cross-stage RTL smoke (опционально): единый тест HistoryPage с полным store snapshot
